import texts from "./texts";
import embeds from "./embeds";
import codes from "./codes";
import lists from "./lists";

const components = { ...texts, ...embeds, ...codes, ...lists };

export default components;
